<div class="blogs">
  {{#each this}}
  <section class="blog">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{blog_text}}
    </div>
  </section>
  {{/each}}
</div>